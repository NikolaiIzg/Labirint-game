import React from 'react'

export default function Box({
               gameStart,
               isWin,
               checkWin,
               box,
               playerStart,
               row,
               col
}) {
  return (
    <div> Box </div>
  )
}
